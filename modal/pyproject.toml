[project]
name = "sleeve-extractor"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.128.0",
  "modal==1.4.3",
  "pydantic==2.12.5",
]

[dependency-groups]
dev = [
  "pytest==9.0.2",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["."]
